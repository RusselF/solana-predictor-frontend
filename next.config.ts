import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  turbopack: {
    // disable strict effect warnings
  },
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "X-Frame-Options", value: "DENY" },
        ],
      },
    ];
  },
};

export default nextConfig;